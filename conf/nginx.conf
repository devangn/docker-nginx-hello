events { }
http {
    server {
      listen 8089 default_server;
      listen [::]:8089 default_server;
  
      location /api/ {
        return 200 '{ "message": "hello world" }';
      }

      location / {
        root /src;
      }
    }
}
